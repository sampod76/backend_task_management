export const RULE_SEARCHABLE_FIELDS = ['name', 'eventType'] as const;

// RuleQueryFilterSchema fields in add  RULE_FILTERABLE_FIELDS

export const RULE_SORTABLE_FIELDS = [
  'priority',
  'createdAt',
  'updatedAt',
  'name',
];
